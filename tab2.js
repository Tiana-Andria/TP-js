//creer un tableau vide
let tableau=[];
//ajout d'elements
let nombre;
for(let i=0;i<nombre;i++){
    tableau.push(i);
}
//trier le tableau
tableau.sort();
//inverser les elements
tableau.reverse();
//verifier si un element specifique existe
let element;
console.log(tableau.includes(element));
